{
  "name": "cin7dsl",
  "version": "1.0.0",
  "private": true,
  "description": "Cin7 DSL - Enterprise React framework integrating Polaris and ExtJS",
  "scripts": {
    "build:all": "npm run build:polaris && npm run copy:files",
    "build:polaris": "cd polaris && pnpm install && cd polaris.shopify.com && pnpm build",
    "copy:files": "mkdir -p dist && cp -r polaris/polaris.shopify.com/.next/standalone/* dist/ && cp index.html dist/ && mkdir -p dist/polaris && cp -r polaris/polaris.shopify.com/.next/standalone/* dist/polaris/",
    "dev": "pnpm --filter \"@cin7/*\" dev",
    "build": "pnpm --filter \"@cin7/*\" build",
    "test": "pnpm --filter \"@cin7/*\" test",
    "lint": "pnpm --filter \"@cin7/*\" lint",
    "typecheck": "pnpm --filter \"@cin7/*\" typecheck",
    "collect:variations": "node scripts/collect-component-variations.js",
    "check:variations": "node scripts/collect-component-variations.js --check"
  },
  "engines": {
    "node": ">=20.10.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.1",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "js-yaml": "^4.1.0",
    "ts-jest": "^29.4.4",
    "typescript": "^5.3.3"
  }
}
